import { LikeBtnChangeDirective } from './like-btn-change.directive';

describe('LikeBtnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeBtnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
